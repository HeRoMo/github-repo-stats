# frozen_string_literal: true

RSpec.describe GithubRepoStats do
  it 'has a version number' do
    expect(GithubRepoStats::VERSION).not_to be_nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
